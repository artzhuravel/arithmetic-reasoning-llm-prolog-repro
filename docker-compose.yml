services:
  trainer:
    build:
      context: .
      dockerfile: Dockerfile
    image: arithmetic-reasoning-llm-prolog-repro:gpu
    working_dir: /workspace
    volumes:
      - ./:/workspace
      - hf-cache:/workspace/.cache/huggingface
    environment:
      TOKENIZERS_PARALLELISM: "false"
    gpus: all
    shm_size: "16gb"
    stdin_open: true
    tty: true
    command: bash

volumes:
  hf-cache:
